[providers]
  node = {}
  python = {}

[phases.build]
  cmds = [
    "npm install",
    "npm run build",
    "cd python_backend && pip install -r requirements.txt"
  ]

[start]
  cmd = "npx concurrently \"npm start\" \"cd python_backend && python app.py\""
